void main() {
  var firstName = 'Srishu'; //can assign any other value too
  String lastName =
      'Chintakindi'; //cannot assign anyother value other then a string

  print(firstName + ' ' + lastName);
}
